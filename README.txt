Instructions to run the code:
make classifier

Alternatively, simply executing 'python classifier.py' on the terminal will execute
the code.

It takes approximately 8 minutes to run completely on the stdlinux server.
